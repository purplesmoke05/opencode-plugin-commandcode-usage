{
  "name": "opencode-plugin-commandcode-usage",
  "version": "0.1.0",
  "description": "opencode plugin that shows CommandCode credits and window limits via /commandcode_usage",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1",
    "solid-js": "1.9.12",
    "@opentui/solid": "0.5.9",
    "@opentui/core": "0.5.9"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.4",
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {}
}